# Assets

Brand and marketing assets that are not part of the frontend application bundle.

- `brand/` — logos, wordmarks, color/type specimens.
- `screenshots/` — product screenshots for documentation, README, and marketing use.

Assets consumed directly by the frontend application at runtime belong in `frontend/src/assets/` instead.
